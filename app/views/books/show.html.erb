 <% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>

<div class="container">
  <div class="show-content">
  <p>Title:<%= @book.title %></p>
  <p>Body:<%= @book.body %></p>
  </div>

 <%= link_to "Edit", "/books/#{@book.id}/edit" %> | <%= link_to "Back", "/books" %>
</div>
